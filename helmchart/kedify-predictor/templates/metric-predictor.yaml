{{- if .Values.kedaPredictionController.enabled }}
{{ $currentScope := . }}
  {{- range $path, $_ := .Files.Glob "metric-predictor-files/**" }}
    {{- with $currentScope }}
---
{{ tpl ($.Files.Get $path) . }}
---
    {{- end }}
  {{- end }}
{{- end }}
