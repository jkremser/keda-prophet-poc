apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keda-prophet.fullname" . }}-test-connection"
  labels:
    {{- include "keda-prophet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keda-prophet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
