apiVersion: v1
kind: Service
metadata:
  name: {{ include "keda-prophet.fullname" . }}
  labels:
    {{- include "keda-prophet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keda-prophet.selectorLabels" . | nindent 4 }}
